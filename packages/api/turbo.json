{
	"$schema": "https://turborepo.org/schema.json",
	"extends": ["//"],
	"tasks": {
		"build": {
			"cache": false,
			"outputs": ["dist/**"],
			"inputs": ["codegen.ts", "graphql/**", "schema.graphql"],
			"dependsOn": ["^build"],
			"env": ["NEXT_PUBLIC_GRAPH_BASE"]
		}
	}
}
