query GetAccessPassDetails($accessPassId: ID!) {
	accessPass: publicAccessPass(id: $accessPassId) {
		...AccessPassFragment
		bannerImage {
			sourceUrl
		}
		headline
		company {
			id
			title
		}
		reviewsAverage
		ownerUser {
			...BasePublicProfileUser
		}
	}
}
