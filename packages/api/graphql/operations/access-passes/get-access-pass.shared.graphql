query getAccessPass($accessPassId: ID!) {
	accessPass: publicAccessPass(id: $accessPassId) {
		...AccessPassFragment
		bannerImage {
			url: optimizedUrl
		}
		headline
		company {
			id
			title
		}
		reviewsAverage
		ownerUser {
			...BasePublicProfileUser
		}
	}
}
