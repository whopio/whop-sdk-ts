mutation sendDirectMessageToUser(
	$toUserIdOrUsername: ID!
	$message: String!
	$attachments: [AttachmentInput!]
) {
	sendMessage(
		input: {
			message: $message
			toUserId: $toUserIdOrUsername
			attachments: $attachments
		}
	)
}
