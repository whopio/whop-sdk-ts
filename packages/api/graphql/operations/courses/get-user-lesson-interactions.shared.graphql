query getUserLessonInteractions($experienceId: ID!) {
	course(experienceId: $experienceId) {
		chapters {
			id
			lessons {
				id
				lessonInteraction {
					id
					completed
					createdAt
				}
			}
		}
	}
}
