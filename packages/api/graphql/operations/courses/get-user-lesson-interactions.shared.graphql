query getUserLessonInteractions($experienceId: ID!, $lessonId: ID!) {
	course(experienceId: $experienceId) {
		chapters {
			lessons {
				lessonInteraction {
					id
					completed
				}
			}
		}
	}
}
