# /courses/:courseId/user_lesson_interactions
query getUserLessonInteractions($courseId: ID!) {
	course(id: $courseId) {
		chapters {
			id
			lessons {
				id
				lessonInteraction {
					id
					completed
					createdAt
				}
			}
		}
	}
}
