query getUser($userId: ID!) {
	publicUser(id: $userId) {
		...BasePublicProfileUser
		bio
		phoneVerified
		banner {
			sourceUrl
		}
		createdAt
		userStat {
			moneyEarned24Hours
			moneyEarned30Days
			moneyEarned7Days
			moneyEarnedLifetime
		}
	}
}
