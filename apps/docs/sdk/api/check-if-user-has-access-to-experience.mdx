---
title: Check if user has access to experience
description: Check if a user has access to a specific experience and their access level.
---

```typescript
import { whopApi } from "@/lib/whop-api";

const { hasAccess, accessLevel } = (await whopApi.checkIfUserHasAccessToExperience({experienceId: "exp_xxx", userId: "user_xxx"})).hasAccessToExperience;

console.log(hasAccess);
console.log(accessLevel);
```
