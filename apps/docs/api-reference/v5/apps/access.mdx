---
openapi: access get /check/{access_expression}
---

---

## Usage

This endpoint is used to check if the current user has access to a given access expression. It is used in the `hasAccess` function of the SDK. You can find more information about access expressions in the [Expressions](/sdk/has-access#expressions) section.
